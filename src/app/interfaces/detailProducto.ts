export interface DetailProduct {
  selerId: number;
  brand: string;
  freeShipping: boolean;
  logisticsType: string;
  sellerPlaceOperation: string;
  itemCondition: string;
  priceRange: number;
  image: string;
}
